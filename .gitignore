# ... existing code ...

# Platform check override (development only)
# Note: vendor/composer/platform_check.php is modified to allow PHP 8.2
# This will be overwritten on composer update - restore manually if needed
node_modules

# Environment files
# .env - Keep in repository for server, but skip local changes using: git update-index --skip-worktree .env
.env.sample

# Laravel hot reload file
public/hot

# Archive files
tpmtambahOEE.zip
